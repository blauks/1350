import React from 'react';
import './TopBar.css';

const TopBar = props => {
  return (
    <div className="topbar">
      <p className="topbarTitle">Bygda i bygda</p>
    </div>
  );
};

export default TopBar;
